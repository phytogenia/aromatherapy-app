import 'package:flutter/material.dart';

class TestRevenueCat extends StatelessWidget {
  const TestRevenueCat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Hello world RevenueCat')),
    );
  }
}
